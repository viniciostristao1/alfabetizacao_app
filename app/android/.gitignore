gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# EXCEÇÃO: debug.keystore versionado de propósito (credenciais padrão do Android
# debug, não são segredo) → assinatura ESTÁVEL no CI (evita "conflito com pacote
# existente" na atualização). NÃO é a keystore de upload da Play Store.
!app/debug.keystore
